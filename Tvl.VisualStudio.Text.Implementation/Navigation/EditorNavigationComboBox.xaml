<ComboBox x:Class="Tvl.VisualStudio.Text.Navigation.Implementation.EditorNavigationComboBox"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          mc:Ignorable="d" 
          d:DesignHeight="300" d:DesignWidth="300"
          IsEnabled="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=HasItems, Mode=OneWay}">

    <ComboBox.ItemTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal">
                <Image Height="16" Width="16" Source="{Binding Glyph}" />
                <TextBlock Text="{Binding Name}" Padding="0" />
            </StackPanel>
        </DataTemplate>
    </ComboBox.ItemTemplate>
</ComboBox>
